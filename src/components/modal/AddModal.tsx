import React from "react";
import { BasicModal } from "components/modal/BasicModal";

export const AddModal = () => {
  return (
    <BasicModal>
      <div>
        <h1>Add modal</h1>
        <button>Save</button>
      </div>
    </BasicModal>
  );
};
